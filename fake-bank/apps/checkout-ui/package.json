{
  "name": "@fake-bank/checkout-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host ${CHECKOUT_UI_HOST:-localhost} --port ${CHECKOUT_UI_PORT:-5173}",
    "build": "vite build",
    "preview": "vite preview --host ${CHECKOUT_UI_HOST:-localhost} --port ${CHECKOUT_UI_PORT:-5173}",
    "test": "echo \"Checkout UI tests are not implemented yet.\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.2",
    "typescript": "^5.9.3",
    "vite": "^7.2.7"
  }
}
